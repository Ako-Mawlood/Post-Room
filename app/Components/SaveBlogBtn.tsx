"use client"

import {CiBookmark as SaveIcon} from "react-icons/ci"

const SaveBlogBtn = ({blogId}: {blogId: string}) => {
  return <SaveIcon onClick={() => {}} className="absolute top-2 right-2 size-5 cursor-pointer" />
}

export default SaveBlogBtn
